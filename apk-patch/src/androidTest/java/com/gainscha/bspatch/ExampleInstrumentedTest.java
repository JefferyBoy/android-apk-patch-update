package com.gainscha.bspatch;

import androidx.test.ext.junit.runners.AndroidJUnit4;

import org.junit.Test;
import org.junit.runner.RunWith;

/**
 * Instrumented test, which will execute on an Android device.
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
@RunWith(AndroidJUnit4.class)
public class ExampleInstrumentedTest {
    @Test
    public void useAppContext() {
//        File oldFile = new File(getExternalFilesDir("apk"), "old.apk");
//        File patchFile = new File(getExternalFilesDir("apk"), "patch.apk");
//        File newFile = new File(getExternalFilesDir("apk"), "new.apk");
//        if (oldFile.exists() && patchFile.exists()) {
//            BsPatch.patch(oldFile.getAbsolutePath(), newFile.getAbsolutePath(), patchFile.getAbsolutePath());
//        }
    }
}